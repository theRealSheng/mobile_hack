import { StyleSheet } from 'react-native';
import { Space } from 'ui/styles';

export default StyleSheet.create({
  input: {
    height: 40,
    borderBottomWidth: 1,
    padding: Space.sm,
    marginBottom: 12,
  },
});
